using OxCoin.Models;
using OxCoin.Services;
using System;
using System.Collections.Generic;
using System.Linq;
using Xunit;

namespace OxCoin.Tests
{
    public class CoinServiceTests
    {
        //private readonly CoinService _coinService;

        //public CoinServiceTests()
        //{
        //    _coinService = new CoinService();
        //}

        //[Fact]
        //public void First()
        //{
        //    var transactions = new List<Transaction> {new Transaction(), new Transaction(), new Transaction()};
        //    var result = _coinService.CreateBlock(transactions);

        //    Assert.IsType<Block>(result);
        //    Assert.IsType<BlockHeader>(result.Header);
        //    Assert.True(result.TransactionCount == 3);
        //    Assert.True(result.Transactions.Count() == 3);
        //}
    }
}
